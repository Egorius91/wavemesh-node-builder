diff --git a/scripts/lib/runtime_state.py b/scripts/lib/runtime_state.py
index f96b0b1..ed06fb2 100644
--- a/scripts/lib/runtime_state.py
+++ b/scripts/lib/runtime_state.py
@@ -8,6 +8,9 @@ import os
 import tempfile
 from datetime import datetime, timezone
 from pathlib import Path
+import sys
+sys.path.insert(0, str(Path(__file__).resolve().parent))
+from route_presentation import route_is_public
 
 
 def utc_now():
@@ -95,7 +98,7 @@ def mutate(config, operation, target, force=False):
 
 
 def inbound_payload(config, item):
-    published = item.get("kind") != "auto" or item.get("presentation", {}).get("published", False)
+    published = route_is_public(config, item, manual_default=item.get("kind") != "auto")
     clients = []
     for client in config.get("clients", []) if published else []:
         credential = next((value for value in client.get("credentials", []) if value.get("route_id") == item["id"]), None)
@@ -150,7 +153,10 @@ def write_artifacts(config, route_id, inbound_path, outbound_path):
 
 def set_inbound_id(config, route_id, inbound_id):
     result = copy.deepcopy(config)
-    route(result, route_id)["entry"]["inbound_id"] = int(inbound_id)
+    item = next((value for value in result.get("routes", []) if value.get("id") == route_id), None)
+    if not item or item.get("kind") not in ("cascade", "auto"):
+        raise ValueError(f"managed route not found: {route_id}")
+    item["entry"]["inbound_id"] = int(inbound_id)
     return result
 
 
@@ -239,6 +245,7 @@ def evaluate(config, previous, inbounds_response, template, nginx_text, subscrip
             continue
         route_id = item["id"]
         enabled = item.get("enabled", True)
+        public = route_is_public(config, item, manual_default=True)
         entry = item["entry"]
         routing = item["routing"]
         inbound = actual_by_tag.get(entry["inbound_tag"])
@@ -266,11 +273,11 @@ def evaluate(config, previous, inbounds_response, template, nginx_text, subscrip
             else:
                 target = subscriptions / "users" / f"{client.get('subscription_id', '')}.txt"
                 found = target.exists() and credential["uuid"] in target.read_text(encoding="utf-8")
-            expected = enabled and client.get("enabled", True) and credential.get("enabled", True)
+            expected = enabled and public and client.get("enabled", True) and credential.get("enabled", True)
             if found != expected:
                 profile_matches = False
         probe = route_probes.get(route_id, {})
-        structural = inbound_matches and inbound_enabled == enabled and rule_present and outbound_matches and nginx_present == enabled and profile_matches
+        structural = inbound_matches and inbound_enabled == enabled and rule_present and outbound_matches and nginx_present == (enabled and public) and profile_matches
         success = structural and probe.get("route_test") is True and probe.get("test_outbound") is True
         misconfigured = enabled and not structural
         old = old_exits.get(item.get("exit_id"), {})
@@ -286,7 +293,7 @@ def evaluate(config, previous, inbounds_response, template, nginx_text, subscrip
             "routing_rule": "present" if rule_present else "missing",
             "route_test_outbound": probe.get("route_test_outbound"),
             "nginx_location": "present" if nginx_present else "missing",
-            "subscription_profile": ("present" if enabled else "absent") if profile_matches else "drifted",
+            "subscription_profile": ("present" if enabled and public else "absent") if profile_matches else "drifted",
         }
         if status in ("unhealthy", "misconfigured"):
             result["node_status"] = "unhealthy"
@@ -306,9 +313,10 @@ def drift_plan(config, runtime):
             actions.append({"route_id": item["id"], "component": "inbound", "action": "reconcile"})
         if enabled and (observed.get("outbound") != "present" or observed.get("routing_rule") != "present"):
             actions.append({"route_id": item["id"], "component": "xray", "action": "reconcile-managed-route"})
-        if observed.get("nginx_location") != ("present" if enabled else "missing"):
+        public = route_is_public(config, item, manual_default=True)
+        if observed.get("nginx_location") != ("present" if enabled and public else "missing"):
             actions.append({"route_id": item["id"], "component": "nginx", "action": "render-desired"})
-        if observed.get("subscription_profile") != ("present" if enabled else "absent"):
+        if observed.get("subscription_profile") != ("present" if enabled and public else "absent"):
             actions.append({"route_id": item["id"], "component": "subscription", "action": "rebuild"})
     return actions
 
diff --git a/scripts/lib/subscription_renderer.py b/scripts/lib/subscription_renderer.py
index 2433788..a5da725 100644
--- a/scripts/lib/subscription_renderer.py
+++ b/scripts/lib/subscription_renderer.py
@@ -1,6 +1,9 @@
 #!/usr/bin/env python3
 import argparse, json, os, secrets, tempfile, urllib.parse
 from pathlib import Path
+import sys
+sys.path.insert(0, str(Path(__file__).resolve().parent))
+from route_presentation import route_is_public
 
 def atomic(path,data):
     path=Path(path); path.parent.mkdir(parents=True,exist_ok=True); fd,tmp=tempfile.mkstemp(prefix=f".{path.name}.",dir=path.parent)
@@ -40,7 +43,7 @@ def render(cfg,output_dir):
         if not client.get("enabled",True): continue
         profiles=[]
         credentials={x["route_id"]:x for x in client.get("credentials",[]) if x.get("enabled",True)}
-        ordered=sorted((r for r in routes.values() if ((r.get("kind")=="cascade" and r.get("exit_id") in exits) or (r.get("kind")=="auto" and r.get("presentation",{}).get("published",False))) and r["id"] in credentials),key=lambda r:(r.get("sort_order",0),r.get("display_name",""),r["id"]))
+        ordered=sorted((r for r in routes.values() if ((r.get("kind")=="cascade" and r.get("exit_id") in exits) or r.get("kind")=="auto") and route_is_public(cfg,r,manual_default=r.get("kind")=="cascade") and r["id"] in credentials),key=lambda r:(r.get("sort_order",0),r.get("display_name",""),r["id"]))
         for route in ordered:
             credential=credentials[route["id"]]; path=route["entry"]["public_path"]
             if route.get("kind")=="cascade": name=route.get("display_name") or exits[route["exit_id"]].get("display_name") or route["id"]
