# Keep the CLI/Agent lock inode stable, including across service restarts.
# Type f creates a missing file without truncating an existing file (never f+).
f /run/lock/wavemesh-node.lock 0600 root root -
